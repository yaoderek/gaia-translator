# Required: exact names (Vite only exposes vars starting with VITE_)
VITE_SUPABASE_URL=https://yourproject.supabase.co
# Use the "anon" / "public" key from Supabase → Project Settings → API (NOT service_role)
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here
